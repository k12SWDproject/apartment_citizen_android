<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectswd">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".activities.OrderDetailActivity"></activity>
        <activity android:name=".activities.CartActivity" />
        <activity android:name=".activities.ServiceActivity" />
        <activity android:name=".activities.DetailPayedActivity" />
        <activity android:name=".activities.DetailActivity" />
        <activity android:name=".activities.OrtherActivity" />
        <activity android:name=".activities.WifiActivity" />
        <activity android:name=".activities.RegisterAcitvity" />
        <activity android:name=".activities.LoginGoogleActivity" />

        <activity android:name=".activities.ManageMembersActivity" />

        <activity android:name=".activities.WaterActivity" />
        <activity android:name=".activities.ElectricActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MenuActivity" />

    </application>

</manifest>